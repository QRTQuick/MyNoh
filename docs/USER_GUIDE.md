# User Guide

- Open Mynoh and use **Capture** to save bugs, solutions, snippets and tags.
- Use **Search** with queries like "Find the React routing issue".
- Review **Timeline** for chronological memory cards.
- Configure GitHub username, token, repository and branch in **Settings**.
- Export knowledge using `ExportService` or future UI export buttons.
