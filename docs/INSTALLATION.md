# Installation Guide

1. Install Python 3.13+.
2. Clone the repository.
3. Create a virtual environment.
4. Run `pip install -e '.[dev]'`.
5. Start with `python main.py`.
6. Optional: run `python scripts/install_startup.py` to register startup behavior.
